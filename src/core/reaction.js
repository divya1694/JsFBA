
export default class Reaction {
	constructor (id, name, subsystem, lowerBound, upperBound) {
		this.id = id
		this.name = name
		this.subsystem = subsystem
		this.lowerBound = lowerBound
		this.upperBound = upperBound
	}
}